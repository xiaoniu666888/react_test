import React from 'react';
import ReactDOM from 'react-dom/client';
import App from '../src/01_组件通信-父传子/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <App />
);


